# 微信小程序获取用户信息（用户名+头像）实操笔记

## 一、核心需求

通过点击按钮，获取当前打开小程序的用户微信名称（nickname）和头像（avatarUrl），并在页面上展示；同时解决实操中的this指向问题、默认头像设置问题。

## 二、核心知识点与实操步骤

### （一）页面结构搭建（前端）

需包含3个核心元素：用户名称展示、头像展示、触发按钮，均与后台data数据绑定。

1. 用户名称展示：用普通文本标签，绑定data中的name变量，默认值为空字符。

2. 头像展示：用image标签，绑定data中的avatar变量，设置默认头像路径，同时添加基础样式（宽高）。

3. 触发按钮：用view标签（可替换为button），绑定tap事件（bindtap），触发getUsername方法。

关键说明：所有展示内容均需与后台data变量绑定，避免写死，才能通过setData动态修改。

### （二）后台逻辑编写（JS）

#### 1. 初始化data数据

在data中定义两个核心变量，用于存储用户信息，设置默认值：

- name：存储用户微信名称，默认值为空字符（''）；

- avatar：存储用户头像路径，默认值为本地静态资源中的默认头像（如static/default.png）。

#### 2. 定义触发方法（getUsername）

点击按钮后，执行该方法，核心是调用微信官方接口获取用户信息，步骤如下：

1. 解决this指向问题（重点易错点）：

2. 在getUsername方法内部，先定义that = this，将外层this（指向页面实例，包含setData方法）赋值给that；

3. 后续在接口回调函数中，使用that替代this，避免回调函数内this指向改变（回调函数内this不包含setData方法）。

4. 调用微信官方接口：wx.getUserInfo()

5. 该接口是微信提供的系统接口，用于获取当前登录用户的基础信息（无需额外配置，登录授权后即可调用）；

6. 接口参数为一个对象，包含两个回调函数：success（调用成功）、fail（调用失败）。

7. 回调函数处理：

8. success回调：接口调用成功后，微信会返回res对象，用户信息存储在res.userInfo中；

9. 从res.userInfo中提取所需数据：用户名（res.userInfo.nickname）、头像路径（res.userInfo.avatarUrl）；

10. 通过that.setData()方法，将提取到的信息赋值给data中的name和avatar变量，实现页面实时更新；

11. fail回调：接口调用失败时触发（如未授权、系统错误），可打印错误信息用于排查。

### （三）默认头像设置细节

1. 准备默认头像图片（如default.png），放入小程序的static文件夹中（用于存储静态资源）；

2. 在data的avatar变量中，默认值设置为该图片路径（如'/static/default.png'）；

3. 给image标签添加style样式，设置固定宽高（如width: 200rpx; height: 200rpx;），避免默认空白占位异常。

## 三、关键易错点（重点复习）

1. this指向问题：回调函数内部不能直接使用this.setData()，必须先将外层this赋值给that，用that.setData()；

2. 数据绑定：页面标签必须与data变量绑定，不能写死路径/内容，否则无法通过setData动态修改；

3. 接口调用：wx.getUserInfo()需用户登录授权后才能调用，若出现系统错误，可检查授权状态或重启开发者工具；

4. 默认头像路径：确保图片放入正确的静态资源文件夹，路径书写正确（相对路径/绝对路径均可）。

## 四、补充说明（老师提及，后续讲解）

1. openSetting相关操作：暂不深入，先按上述步骤编写，后续单独讲解；

2. this与that的详细区别：后续单独拆解，重点记住当前实操的用法即可。

## 五、实操要求

在自己的开发工具中，完整实现上述步骤：搭建页面结构、编写JS逻辑、测试按钮触发效果，确保能成功获取并展示用户名称和头像。
> （注：文档部分内容可能由 AI 生成）