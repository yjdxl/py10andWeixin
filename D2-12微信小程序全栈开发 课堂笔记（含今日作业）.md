# 微信小程序全栈开发 课堂笔记（含今日作业）

# 一、今日核心作业（重点）

## （一）必做页面（仅写HTML、CSS，先完成页面结构）

1. **拍卖详细页面**

    - 前提：已完成拍卖列表页面，点击列表项可跳转至该详细页面（此前仅打印了ID）

    - 要求：参考老师后续发送的截图，完成页面布局，需结合课堂所讲知识点应用

2. **发布消息页面（类似朋友圈发布页面）**

    - 功能对应：模拟发朋友圈的操作逻辑

    - 页面需包含：选择图片、输入文字、选择地理位置的相关布局

    - 关键标签：需使用微信小程序的`textarea`标签（多行文本框，区别于单行输入框，用于输入多行文字，贴合朋友圈文案输入场景）

## （二）功能点添加（页面完成后，补充JS与API相关功能）

1. **拍卖列表页面功能优化**

    - 给拍卖列表中的每一项绑定时间

    - 数据优化：此前拍卖列表数据为写死状态，需改为对接后端数据

    - 实现方式：使用微信小程序的`for`循环指令，渲染后端返回的拍卖列表数据，实现数据动态展示（后端返回什么数据，前端就展示什么数据）

2. **拍卖列表→拍卖详细页面 跳转传参**

    - 跳转逻辑：点击拍卖列表中的某一项，需将该列表项的「ID」传递给拍卖详细页面

    - 接收方式：在拍卖详细页面，通过课堂讲解的`options`参数（老师口误提及的unload、ocean均为口误，核心是options参数）获取传递过来的ID

    - 补充：该传参方式属于课堂初始讲解的跳转传参第一种方式

3. **发布消息页面功能实现**

    - 功能1：图片挑选与预览、图片上传功能（复用今日课堂所讲的图片相关API）

    - 功能2：地理位置选择功能（复用今日课堂所讲的地理位置相关API）

## （三）作业要求

- 先完成页面结构（仅HTML、CSS），再添加JS与API功能，不可颠倒顺序

- 严格按照课堂所讲知识点应用，不可随意编写

- 今日作业必须完成

- 后续老师会发送相关页面截图，供参考布局

# 二、今日课堂核心知识点（关联作业，重点记忆）

1. **微信小程序跳转传参**：点击列表项跳转至详情页时，可通过`options`参数传递当前项的ID，详情页通过该参数接收ID

2. **for循环指令**：用于渲染后端返回的列表数据，实现数据动态展示，替代写死数据的方式

3. **textarea标签**：微信小程序中的多行文本框标签，用于输入多行文字（如朋友圈文案），区别于单行输入框

4. **核心API应用**：图片挑选、预览、上传API，地理位置选择API（需在发布消息页面复用）

# 三、易错点提醒

- 老师口误纠正：讲解跳转传参时，提及的「unload」「ocean」均为口误，正确的传参接收参数是`options`

- 区分标签：`textarea`是多行文本框，用于发布消息页面的文案输入，不可用单行输入框替代

- 数据逻辑：拍卖列表数据需对接后端，通过for循环渲染，不可继续写死数据
> （注：文档部分内容可能由 AI 生成）